#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../express-mongoose-generator/bin/mongoose-gen" "$@"
  ret=$?
else 
  node  "$basedir/../express-mongoose-generator/bin/mongoose-gen" "$@"
  ret=$?
fi
exit $ret
